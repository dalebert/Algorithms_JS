// Use arrays when order matters
// Complcates insertion and removal sometimes

// Big O of arrays
// Accessing - O(1)
// Insertion depends on order
    // Add to end (push) is fast - O(1)
    // Add to beginning is slow because requires re-indexing
// Removing is similar
    // Removing from end (pop) is fast - O(1)
    // Removing from beginning also requires re-indexing
// Searching unordered - O(n)
// Searching ordered - O(logN))
